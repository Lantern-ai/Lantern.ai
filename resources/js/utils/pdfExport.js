// PDF export using browser print
export const exportToPDF = () => {
  window.print();
};

// Note: For full PDF generation with custom formatting,
// you would need a library like jsPDF or pdfmake
// This is a simple implementation using browser print